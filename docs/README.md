# docs/

Planning notes, audit reports, and research writeups that aren't read by any
script, page, or automation — moved here purely to declutter the repo root.

Two files were deliberately left at the repo root instead of here:
`blog-cluster-report.md` and `blog-gsc-per-page.md` — CLAUDE.md,
`skills/blog-write/SKILL.md`, `skills/seo-audit/SKILL.md`, and
`blog/topic-ledger.md` all point to them "in the repo root" by name, so moving
them would break those instructions.
